import styled from 'styled-components';

export const Main = styled.main`
  background-color: ${p=>p.theme.colors.thickBlack}; 
`; 
export const MainArticle = styled.article`
  margin: 0 auto;
  width: 1024px; 
`;